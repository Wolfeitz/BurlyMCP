# Burly MCP Server Configuration
# Copy this file to .env and customize the values for your environment

# =============================================================================
# GOTIFY NOTIFICATIONS (Optional)
# =============================================================================
# Enable/disable Gotify notifications for tool execution alerts
GOTIFY_ENABLED=false

# Gotify server URL (include protocol and port if non-standard)
# Example: https://gotify.example.com or http://localhost:8080
GOTIFY_URL=

# Gotify application token for authentication
# Generate this in your Gotify web interface under Applications
GOTIFY_TOKEN=

# =============================================================================
# SECURITY SETTINGS
# =============================================================================
# Blog staging directory (read-only for validation)
# This is where blog posts are staged before publishing
BLOG_STAGE_ROOT=/app/data/blog/stage

# Blog publish directory (write-only for publishing)
# This is where validated blog posts are published
BLOG_PUBLISH_ROOT=/app/data/blog/public

# Maximum output size before truncation (in bytes)
# Prevents memory exhaustion from tools with excessive output
OUTPUT_TRUNCATE_LIMIT=10240

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================
# Log level for application logging
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Path to audit log file (JSON Lines format)
# All tool executions are logged here for security monitoring
AUDIT_LOG_PATH=/var/log/agentops/audit.jsonl

# =============================================================================
# DEVELOPMENT SETTINGS (Development only - not for production)
# =============================================================================
# Enable debug mode (DO NOT use in production)
# DEBUG=false

# Development server port (if running outside container)
# DEV_PORT=8000

# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================
# Docker socket path (usually doesn't need to be changed)
# DOCKER_SOCKET=/var/run/docker.sock

# =============================================================================
# NOTES
# =============================================================================
# 1. Never commit this file with real secrets to version control
# 2. Use strong, unique tokens for Gotify authentication
# 3. Ensure file paths exist and have proper permissions
# 4. Review security settings before deploying to production
# 5. Monitor audit logs regularly for security events